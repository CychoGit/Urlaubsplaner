# ============================================
# Team Urlaubsplaner - Umgebungsvariablen
# ============================================
#
# Kopieren Sie diese Datei zu ".env" und passen Sie die Werte an:
#   cp .env.example .env
#
# WICHTIG: .env-Datei niemals in Git committen!

# ============================================
# PostgreSQL Datenbank-Konfiguration
# ============================================
# WICHTIG: 'vacation_admin' ist der technische Datenbank-User,
# NICHT der Super-Admin (Tenant Admin) der Anwendung!
# Der Tenant Admin wird beim ersten Start automatisch erstellt.

# Vollständige Datenbank-URL (Neon, lokale PostgreSQL, etc.)
DATABASE_URL="postgresql://vacation_admin:IhrSicheresPasswort@localhost:5432/team_vacation_planner"

# Einzelne PostgreSQL-Parameter (für lokale Entwicklung)
PGHOST="localhost"
PGPORT="5432"
PGUSER="vacation_admin"
PGPASSWORD="IhrSicheresPasswort"
PGDATABASE="team_vacation_planner"

# ============================================
# Session & Sicherheit
# ============================================

# Session-Secret für sichere Cookie-Signierung
# WICHTIG: Generieren Sie einen zufälligen String!
# Befehl: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET="generieren-sie-einen-langen-zufaelligen-string-hier"

# ============================================
# Server-Konfiguration
# ============================================

# Port auf dem die Anwendung läuft
PORT="5000"

# Umgebung: "development" oder "production"
# In production werden Test-Daten NICHT erstellt
NODE_ENV="development"

# Optional: Domain für Cookie-Scope (nur in Produktion mit Nginx)
# COOKIE_DOMAIN="urlaub.ihre-firma.de"

# ============================================
# Object Storage (optional, falls aktiviert)
# ============================================

# Diese Variablen werden automatisch von Replit gesetzt,
# falls Sie Object Storage verwenden. Für Self-Hosting
# müssen Sie diese manuell konfigurieren.

# DEFAULT_OBJECT_STORAGE_BUCKET_ID="your-bucket-id"
# PRIVATE_OBJECT_DIR=".private"
# PUBLIC_OBJECT_SEARCH_PATHS="public"

# ============================================
# Logging & Monitoring (optional)
# ============================================

# Log-Level: "error", "warn", "info", "debug"
# LOG_LEVEL="info"

# ============================================
# Email-Benachrichtigungen (optional, zukünftig)
# ============================================

# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
# EMAIL_FROM="Team Urlaubsplaner <noreply@ihre-firma.de>"

# ============================================
# Entwicklungs-Optionen
# ============================================

# Node.js Memory Limit (für große Anwendungen)
# NODE_OPTIONS="--max-old-space-size=2048"

# ============================================
# Notizen
# ============================================
#
# - Die .env-Datei wird automatisch von der Anwendung geladen
# - In Produktion sollten Sie NODE_ENV=production setzen
# - Verwenden Sie starke, eindeutige Passwörter
# - Sichern Sie .env-Dateien niemals in Git (steht in .gitignore)
# - Bei Cloud-Deployments können Secrets über die Platform verwaltet werden
